type Query {
  newses: [News]
  news(id: ID!): News
}

type News {
  id: ID!
  article: Article
  user: User
  isModer: Boolean!
}

type User {
  id: ID!
  email: String!
}

type Article {
  id: ID!
  title: String,
  text: String
}

